import sys


firstno = float(raw_input('enter a number '))
secondno = float(raw_input('enter a number '))

try :
    result = firstno / secondno
    print result
except :
    error = sys.exc_info()
    print 'An error occured'
    print error